apiVersion: v1
kind: Service
metadata:
  name: wallet-service
  namespace: crypto-platform
  labels:
    app: wallet-service
spec:
  selector:
    app: wallet-service
  ports:
    - name: http
      port: 8083
      targetPort: 8083
  type: ClusterIP
